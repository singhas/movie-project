server.port: 8097
spring.cloud.config.ena
spring:
    cloud:
      config:
        discovery:
          enabled: true
          service-id: config-server
#        uri: http://localhost:8089
#        failFast: true
#        retry:
#          initial-interval: 3000
#          multiplier: 1.3
#          max-interval: 5000
#          max-attempts: 20
    application:
      name: test
